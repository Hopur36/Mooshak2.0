@model Mooshack_2.Models.ViewModels.TeacherFrontPageViewModel
@{
    ViewBag.Title = "Kennari";
}

<h3>
    Active Assignments
</h3>

<body>

    <div class="panel panel-default">
        <div class="panel-body">
            <!--
            Verkefni í gangi
                -->
            <div class="table-responsive">
                <table class="table">
                    <tr>
                        <th>
                            @Html.ActionLink("Title", "TeacherFrontPage", new { sortOrder = "title" }, new { @style = "color:black;" })
                        </th>
                        <th>
                            @Html.ActionLink("Course", "TeacherFrontPage", new { sortOrder = "course" }, new { @style = "color:black;" })
                        </th>
                        <th>
                            @Html.ActionLink("Start Date", "TeacherFrontPage", new { sortOrder = "start" }, new { @style = "color:black;" })
                        </th>
                        <th>
                            @Html.ActionLink("End Date", "TeacherFrontPage", new { sortOrder = "end" }, new { @style = "color:black;" })
                        </th>
                    </tr>
                    @foreach (var _assignment in Model.Assignments)
                    {
                        <tr>
                            <td>
                                @Html.ActionLink(_assignment.Title, "teacherAssignmentMilestonesPage", new { controller = "assignment", assignmentID = _assignment.id })
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => _assignment.CourseName)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => _assignment.StartDateTime)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => _assignment.EndDateTime)
                            </td>
                        </tr>
                    }
                </table>
            </div>

        </div>
    </div>
    <br>
   
</body>
